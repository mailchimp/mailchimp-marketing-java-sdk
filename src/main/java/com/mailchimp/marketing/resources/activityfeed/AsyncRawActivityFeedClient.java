/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.activityfeed;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.QueryStringMapper;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.core.pagination.SyncPagingIterable;
import com.mailchimp.marketing.resources.activityfeed.requests.ListChimpChatterActivityFeedRequest;
import com.mailchimp.marketing.resources.activityfeed.types.ListActivityFeedResponseItem;
import com.mailchimp.marketing.resources.activityfeed.types.ListChimpChatterActivityFeedResponse;
import com.mailchimp.marketing.resources.activityfeed.types.ListChimpChatterActivityFeedResponseChimpChatterItem;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.ExecutionException;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawActivityFeedClient {
    protected final ClientOptions clientOptions;

    public AsyncRawActivityFeedClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get information about the activity feed endpoint's resources.
     */
    public CompletableFuture<MailchimpClientHttpResponse<List<ListActivityFeedResponseItem>>> list() {
        return list(null);
    }

    /**
     * Get information about the activity feed endpoint's resources.
     */
    public CompletableFuture<MailchimpClientHttpResponse<List<ListActivityFeedResponseItem>>> list(
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/activity-feed");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<MailchimpClientHttpResponse<List<ListActivityFeedResponseItem>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new MailchimpClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<ListActivityFeedResponseItem>>() {}),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new MailchimpClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(
                            new MailchimpClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new MailchimpClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Return the Chimp Chatter for this account ordered by most recent.
     */
    public CompletableFuture<
                    MailchimpClientHttpResponse<
                            SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>>>
            listChimpChatter() {
        return listChimpChatter(ListChimpChatterActivityFeedRequest.builder().build());
    }

    /**
     * Return the Chimp Chatter for this account ordered by most recent.
     */
    public CompletableFuture<
                    MailchimpClientHttpResponse<
                            SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>>>
            listChimpChatter(RequestOptions requestOptions) {
        return listChimpChatter(ListChimpChatterActivityFeedRequest.builder().build(), requestOptions);
    }

    /**
     * Return the Chimp Chatter for this account ordered by most recent.
     */
    public CompletableFuture<
                    MailchimpClientHttpResponse<
                            SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>>>
            listChimpChatter(ListChimpChatterActivityFeedRequest request) {
        return listChimpChatter(request, null);
    }

    /**
     * Return the Chimp Chatter for this account ordered by most recent.
     */
    public CompletableFuture<
                    MailchimpClientHttpResponse<
                            SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>>>
            listChimpChatter(ListChimpChatterActivityFeedRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/activity-feed/chimp-chatter");
        if (request.getCount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "count", request.getCount().get(), false);
        }
        if (request.getOffset().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "offset", request.getOffset().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<
                        MailchimpClientHttpResponse<
                                SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>>>
                future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListChimpChatterActivityFeedResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListChimpChatterActivityFeedResponse.class);
                        int newPageNumber = request.getOffset()
                                .map((Integer page) -> page + 1)
                                .orElse(1);
                        ListChimpChatterActivityFeedRequest nextRequest = ListChimpChatterActivityFeedRequest.builder()
                                .from(request)
                                .offset(newPageNumber)
                                .build();
                        List<ListChimpChatterActivityFeedResponseChimpChatterItem> result =
                                parsedResponse.getChimpChatter().orElse(Collections.emptyList());
                        future.complete(new MailchimpClientHttpResponse<>(
                                new SyncPagingIterable<ListChimpChatterActivityFeedResponseChimpChatterItem>(
                                        true, result, parsedResponse, () -> {
                                            try {
                                                return listChimpChatter(nextRequest, requestOptions)
                                                        .get()
                                                        .body();
                                            } catch (InterruptedException | ExecutionException e) {
                                                throw new RuntimeException(e);
                                            }
                                        }),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new MailchimpClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(
                            new MailchimpClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new MailchimpClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
