/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.campaignfolders;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.MediaTypes;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.QueryStringMapper;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.core.pagination.SyncPagingIterable;
import com.mailchimp.marketing.resources.campaignfolders.requests.CreateCampaignFoldersRequest;
import com.mailchimp.marketing.resources.campaignfolders.requests.DeleteCampaignFoldersRequest;
import com.mailchimp.marketing.resources.campaignfolders.requests.GetCampaignFoldersRequest;
import com.mailchimp.marketing.resources.campaignfolders.requests.ListCampaignFoldersRequest;
import com.mailchimp.marketing.resources.campaignfolders.requests.UpdateCampaignFoldersRequest;
import com.mailchimp.marketing.resources.campaignfolders.types.GetCampaignFoldersResponse;
import com.mailchimp.marketing.resources.campaignfolders.types.UpdateCampaignFoldersResponse;
import com.mailchimp.marketing.types.CampaignFolders;
import com.mailchimp.marketing.types.CampaignFoldersFoldersItem;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCampaignFoldersClient {
    protected final ClientOptions clientOptions;

    public RawCampaignFoldersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get all folders used to organize campaigns.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<CampaignFoldersFoldersItem>> list() {
        return list(ListCampaignFoldersRequest.builder().build());
    }

    /**
     * Get all folders used to organize campaigns.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<CampaignFoldersFoldersItem>> list(
            RequestOptions requestOptions) {
        return list(ListCampaignFoldersRequest.builder().build(), requestOptions);
    }

    /**
     * Get all folders used to organize campaigns.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<CampaignFoldersFoldersItem>> list(
            ListCampaignFoldersRequest request) {
        return list(request, null);
    }

    /**
     * Get all folders used to organize campaigns.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<CampaignFoldersFoldersItem>> list(
            ListCampaignFoldersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/campaign-folders");
        if (request.getCount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "count", request.getCount().get(), false);
        }
        if (request.getOffset().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "offset", request.getOffset().get(), false);
        }
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                CampaignFolders parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CampaignFolders.class);
                int newPageNumber =
                        request.getOffset().map((Integer page) -> page + 1).orElse(1);
                ListCampaignFoldersRequest nextRequest = ListCampaignFoldersRequest.builder()
                        .from(request)
                        .offset(newPageNumber)
                        .build();
                List<CampaignFoldersFoldersItem> result =
                        parsedResponse.getFolders().orElse(Collections.emptyList());
                return new MailchimpClientHttpResponse<>(
                        new SyncPagingIterable<CampaignFoldersFoldersItem>(
                                true, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Create a new campaign folder.
     */
    public MailchimpClientHttpResponse<CampaignFolders> create(CreateCampaignFoldersRequest request) {
        return create(request, null);
    }

    /**
     * Create a new campaign folder.
     */
    public MailchimpClientHttpResponse<CampaignFolders> create(
            CreateCampaignFoldersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/campaign-folders");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CampaignFolders.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get information about a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<GetCampaignFoldersResponse> get(String folderId) {
        return get(folderId, GetCampaignFoldersRequest.builder().build());
    }

    /**
     * Get information about a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<GetCampaignFoldersResponse> get(String folderId, RequestOptions requestOptions) {
        return get(folderId, GetCampaignFoldersRequest.builder().build(), requestOptions);
    }

    /**
     * Get information about a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<GetCampaignFoldersResponse> get(
            String folderId, GetCampaignFoldersRequest request) {
        return get(folderId, request, null);
    }

    /**
     * Get information about a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<GetCampaignFoldersResponse> get(
            String folderId, GetCampaignFoldersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/campaign-folders")
                .addPathSegment(folderId);
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetCampaignFoldersResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a specific campaign folder, and mark all the campaigns in the folder as 'unfiled'.
     */
    public MailchimpClientHttpResponse<Void> delete(String folderId) {
        return delete(folderId, DeleteCampaignFoldersRequest.builder().build());
    }

    /**
     * Delete a specific campaign folder, and mark all the campaigns in the folder as 'unfiled'.
     */
    public MailchimpClientHttpResponse<Void> delete(String folderId, RequestOptions requestOptions) {
        return delete(folderId, DeleteCampaignFoldersRequest.builder().build(), requestOptions);
    }

    /**
     * Delete a specific campaign folder, and mark all the campaigns in the folder as 'unfiled'.
     */
    public MailchimpClientHttpResponse<Void> delete(String folderId, DeleteCampaignFoldersRequest request) {
        return delete(folderId, request, null);
    }

    /**
     * Delete a specific campaign folder, and mark all the campaigns in the folder as 'unfiled'.
     */
    public MailchimpClientHttpResponse<Void> delete(
            String folderId, DeleteCampaignFoldersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/campaign-folders")
                .addPathSegment(folderId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Update a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<UpdateCampaignFoldersResponse> update(
            String folderId, UpdateCampaignFoldersRequest request) {
        return update(folderId, request, null);
    }

    /**
     * Update a specific folder used to organize campaigns.
     */
    public MailchimpClientHttpResponse<UpdateCampaignFoldersResponse> update(
            String folderId, UpdateCampaignFoldersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/campaign-folders")
                .addPathSegment(folderId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateCampaignFoldersResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }
}
