/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.landingpages;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.MediaTypes;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.QueryStringMapper;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.resources.landingpages.requests.CreateActionPublishLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.CreateActionUnpublishLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.CreateLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.DeleteLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.GetLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.ListContentLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.ListLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.requests.UpdateLandingPagesRequest;
import com.mailchimp.marketing.resources.landingpages.types.ListContentLandingPagesResponse;
import com.mailchimp.marketing.resources.landingpages.types.ListLandingPagesResponse;
import com.mailchimp.marketing.types.LandingPage;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawLandingPagesClient {
    protected final ClientOptions clientOptions;

    public RawLandingPagesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get all landing pages.
     */
    public MailchimpClientHttpResponse<ListLandingPagesResponse> list() {
        return list(ListLandingPagesRequest.builder().build());
    }

    /**
     * Get all landing pages.
     */
    public MailchimpClientHttpResponse<ListLandingPagesResponse> list(RequestOptions requestOptions) {
        return list(ListLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Get all landing pages.
     */
    public MailchimpClientHttpResponse<ListLandingPagesResponse> list(ListLandingPagesRequest request) {
        return list(request, null);
    }

    /**
     * Get all landing pages.
     */
    public MailchimpClientHttpResponse<ListLandingPagesResponse> list(
            ListLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages");
        if (request.getSortDir().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort_dir", request.getSortDir().get(), false);
        }
        if (request.getSortField().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort_field", request.getSortField().get(), false);
        }
        if (request.getCount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "count", request.getCount().get(), false);
        }
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListLandingPagesResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Create an unpublished and contentless Mailchimp landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> create() {
        return create(CreateLandingPagesRequest.builder().build());
    }

    /**
     * Create an unpublished and contentless Mailchimp landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> create(RequestOptions requestOptions) {
        return create(CreateLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Create an unpublished and contentless Mailchimp landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> create(CreateLandingPagesRequest request) {
        return create(request, null);
    }

    /**
     * Create an unpublished and contentless Mailchimp landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> create(
            CreateLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages");
        if (request.getUseDefaultList().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "use_default_list", request.getUseDefaultList().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LandingPage.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get information about a specific page.
     */
    public MailchimpClientHttpResponse<LandingPage> get(String pageId) {
        return get(pageId, GetLandingPagesRequest.builder().build());
    }

    /**
     * Get information about a specific page.
     */
    public MailchimpClientHttpResponse<LandingPage> get(String pageId, RequestOptions requestOptions) {
        return get(pageId, GetLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Get information about a specific page.
     */
    public MailchimpClientHttpResponse<LandingPage> get(String pageId, GetLandingPagesRequest request) {
        return get(pageId, request, null);
    }

    /**
     * Get information about a specific page.
     */
    public MailchimpClientHttpResponse<LandingPage> get(
            String pageId, GetLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId);
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LandingPage.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a landing page.
     */
    public MailchimpClientHttpResponse<Void> delete(String pageId) {
        return delete(pageId, DeleteLandingPagesRequest.builder().build());
    }

    /**
     * Delete a landing page.
     */
    public MailchimpClientHttpResponse<Void> delete(String pageId, RequestOptions requestOptions) {
        return delete(pageId, DeleteLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Delete a landing page.
     */
    public MailchimpClientHttpResponse<Void> delete(String pageId, DeleteLandingPagesRequest request) {
        return delete(pageId, request, null);
    }

    /**
     * Delete a landing page.
     */
    public MailchimpClientHttpResponse<Void> delete(
            String pageId, DeleteLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Update a landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> update(String pageId) {
        return update(pageId, UpdateLandingPagesRequest.builder().build());
    }

    /**
     * Update a landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> update(String pageId, RequestOptions requestOptions) {
        return update(pageId, UpdateLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Update a landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> update(String pageId, UpdateLandingPagesRequest request) {
        return update(pageId, request, null);
    }

    /**
     * Update a landing page.
     */
    public MailchimpClientHttpResponse<LandingPage> update(
            String pageId, UpdateLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LandingPage.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Publish a landing page that is in draft, unpublished, or has been previously published and edited.
     */
    public MailchimpClientHttpResponse<Void> createActionPublish(String pageId) {
        return createActionPublish(
                pageId, CreateActionPublishLandingPagesRequest.builder().build());
    }

    /**
     * Publish a landing page that is in draft, unpublished, or has been previously published and edited.
     */
    public MailchimpClientHttpResponse<Void> createActionPublish(String pageId, RequestOptions requestOptions) {
        return createActionPublish(
                pageId, CreateActionPublishLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Publish a landing page that is in draft, unpublished, or has been previously published and edited.
     */
    public MailchimpClientHttpResponse<Void> createActionPublish(
            String pageId, CreateActionPublishLandingPagesRequest request) {
        return createActionPublish(pageId, request, null);
    }

    /**
     * Publish a landing page that is in draft, unpublished, or has been previously published and edited.
     */
    public MailchimpClientHttpResponse<Void> createActionPublish(
            String pageId, CreateActionPublishLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId)
                .addPathSegments("actions")
                .addPathSegments("publish");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Unpublish a landing page that is in draft or has been published.
     */
    public MailchimpClientHttpResponse<Void> createActionUnpublish(String pageId) {
        return createActionUnpublish(
                pageId, CreateActionUnpublishLandingPagesRequest.builder().build());
    }

    /**
     * Unpublish a landing page that is in draft or has been published.
     */
    public MailchimpClientHttpResponse<Void> createActionUnpublish(String pageId, RequestOptions requestOptions) {
        return createActionUnpublish(
                pageId, CreateActionUnpublishLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Unpublish a landing page that is in draft or has been published.
     */
    public MailchimpClientHttpResponse<Void> createActionUnpublish(
            String pageId, CreateActionUnpublishLandingPagesRequest request) {
        return createActionUnpublish(pageId, request, null);
    }

    /**
     * Unpublish a landing page that is in draft or has been published.
     */
    public MailchimpClientHttpResponse<Void> createActionUnpublish(
            String pageId, CreateActionUnpublishLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId)
                .addPathSegments("actions")
                .addPathSegments("unpublish");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get the the HTML for your landing page.
     */
    public MailchimpClientHttpResponse<ListContentLandingPagesResponse> listContent(String pageId) {
        return listContent(pageId, ListContentLandingPagesRequest.builder().build());
    }

    /**
     * Get the the HTML for your landing page.
     */
    public MailchimpClientHttpResponse<ListContentLandingPagesResponse> listContent(
            String pageId, RequestOptions requestOptions) {
        return listContent(pageId, ListContentLandingPagesRequest.builder().build(), requestOptions);
    }

    /**
     * Get the the HTML for your landing page.
     */
    public MailchimpClientHttpResponse<ListContentLandingPagesResponse> listContent(
            String pageId, ListContentLandingPagesRequest request) {
        return listContent(pageId, request, null);
    }

    /**
     * Get the the HTML for your landing page.
     */
    public MailchimpClientHttpResponse<ListContentLandingPagesResponse> listContent(
            String pageId, ListContentLandingPagesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/landing-pages")
                .addPathSegment(pageId)
                .addPathSegments("content");
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListContentLandingPagesResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }
}
