/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.smscampaigns;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.MediaTypes;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.QueryStringMapper;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.core.pagination.SyncPagingIterable;
import com.mailchimp.marketing.resources.smscampaigns.requests.CreateActionCancelSendSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.CreateActionScheduleSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.CreateActionSendSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.CreateSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.DeleteSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.GetContentSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.GetSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.ListSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.UpdateSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.requests.UpsertContentSmsCampaignsRequest;
import com.mailchimp.marketing.resources.smscampaigns.types.ListSmsCampaignsResponse;
import com.mailchimp.marketing.types.SmsCampaign;
import com.mailchimp.marketing.types.SmsCampaignContent;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSmsCampaignsClient {
    protected final ClientOptions clientOptions;

    public RawSmsCampaignsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get all SMS campaigns in an account.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<SmsCampaign>> list() {
        return list(ListSmsCampaignsRequest.builder().build());
    }

    /**
     * Get all SMS campaigns in an account.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<SmsCampaign>> list(RequestOptions requestOptions) {
        return list(ListSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Get all SMS campaigns in an account.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<SmsCampaign>> list(ListSmsCampaignsRequest request) {
        return list(request, null);
    }

    /**
     * Get all SMS campaigns in an account.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<SmsCampaign>> list(
            ListSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns");
        if (request.getCount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "count", request.getCount().get(), false);
        }
        if (request.getOffset().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "offset", request.getOffset().get(), false);
        }
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListSmsCampaignsResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListSmsCampaignsResponse.class);
                int newPageNumber =
                        request.getOffset().map((Integer page) -> page + 1).orElse(1);
                ListSmsCampaignsRequest nextRequest = ListSmsCampaignsRequest.builder()
                        .from(request)
                        .offset(newPageNumber)
                        .build();
                List<SmsCampaign> result = parsedResponse.getSmsCampaigns().orElse(Collections.emptyList());
                return new MailchimpClientHttpResponse<>(
                        new SyncPagingIterable<SmsCampaign>(
                                true, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Create a new SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> create(CreateSmsCampaignsRequest request) {
        return create(request, null);
    }

    /**
     * Create a new SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> create(
            CreateSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SmsCampaign.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get the details for a single SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> get(String smsCampaignId) {
        return get(smsCampaignId, GetSmsCampaignsRequest.builder().build());
    }

    /**
     * Get the details for a single SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> get(String smsCampaignId, RequestOptions requestOptions) {
        return get(smsCampaignId, GetSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Get the details for a single SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> get(String smsCampaignId, GetSmsCampaignsRequest request) {
        return get(smsCampaignId, request, null);
    }

    /**
     * Get the details for a single SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> get(
            String smsCampaignId, GetSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId);
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SmsCampaign.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Remove a campaign from your Mailchimp account.
     */
    public MailchimpClientHttpResponse<Void> delete(String smsCampaignId) {
        return delete(smsCampaignId, DeleteSmsCampaignsRequest.builder().build());
    }

    /**
     * Remove a campaign from your Mailchimp account.
     */
    public MailchimpClientHttpResponse<Void> delete(String smsCampaignId, RequestOptions requestOptions) {
        return delete(smsCampaignId, DeleteSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Remove a campaign from your Mailchimp account.
     */
    public MailchimpClientHttpResponse<Void> delete(String smsCampaignId, DeleteSmsCampaignsRequest request) {
        return delete(smsCampaignId, request, null);
    }

    /**
     * Remove a campaign from your Mailchimp account.
     */
    public MailchimpClientHttpResponse<Void> delete(
            String smsCampaignId, DeleteSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Update an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> update(String smsCampaignId) {
        return update(smsCampaignId, UpdateSmsCampaignsRequest.builder().build());
    }

    /**
     * Update an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> update(String smsCampaignId, RequestOptions requestOptions) {
        return update(smsCampaignId, UpdateSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Update an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> update(String smsCampaignId, UpdateSmsCampaignsRequest request) {
        return update(smsCampaignId, request, null);
    }

    /**
     * Update an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaign> update(
            String smsCampaignId, UpdateSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SmsCampaign.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Cancel a scheduled or sending SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionCancelSend(String smsCampaignId) {
        return createActionCancelSend(
                smsCampaignId,
                CreateActionCancelSendSmsCampaignsRequest.builder().build());
    }

    /**
     * Cancel a scheduled or sending SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionCancelSend(
            String smsCampaignId, RequestOptions requestOptions) {
        return createActionCancelSend(
                smsCampaignId,
                CreateActionCancelSendSmsCampaignsRequest.builder().build(),
                requestOptions);
    }

    /**
     * Cancel a scheduled or sending SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionCancelSend(
            String smsCampaignId, CreateActionCancelSendSmsCampaignsRequest request) {
        return createActionCancelSend(smsCampaignId, request, null);
    }

    /**
     * Cancel a scheduled or sending SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionCancelSend(
            String smsCampaignId, CreateActionCancelSendSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId)
                .addPathSegments("actions")
                .addPathSegments("cancel-send");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Schedule an SMS campaign for delivery.
     */
    public MailchimpClientHttpResponse<Void> createActionSchedule(
            String smsCampaignId, CreateActionScheduleSmsCampaignsRequest request) {
        return createActionSchedule(smsCampaignId, request, null);
    }

    /**
     * Schedule an SMS campaign for delivery.
     */
    public MailchimpClientHttpResponse<Void> createActionSchedule(
            String smsCampaignId, CreateActionScheduleSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId)
                .addPathSegments("actions")
                .addPathSegments("schedule");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Send an SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionSend(String smsCampaignId) {
        return createActionSend(
                smsCampaignId, CreateActionSendSmsCampaignsRequest.builder().build());
    }

    /**
     * Send an SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionSend(String smsCampaignId, RequestOptions requestOptions) {
        return createActionSend(
                smsCampaignId, CreateActionSendSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Send an SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionSend(
            String smsCampaignId, CreateActionSendSmsCampaignsRequest request) {
        return createActionSend(smsCampaignId, request, null);
    }

    /**
     * Send an SMS campaign.
     */
    public MailchimpClientHttpResponse<Void> createActionSend(
            String smsCampaignId, CreateActionSendSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId)
                .addPathSegments("actions")
                .addPathSegments("send");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> getContent(String smsCampaignId) {
        return getContent(smsCampaignId, GetContentSmsCampaignsRequest.builder().build());
    }

    /**
     * Get the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> getContent(
            String smsCampaignId, RequestOptions requestOptions) {
        return getContent(smsCampaignId, GetContentSmsCampaignsRequest.builder().build(), requestOptions);
    }

    /**
     * Get the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> getContent(
            String smsCampaignId, GetContentSmsCampaignsRequest request) {
        return getContent(smsCampaignId, request, null);
    }

    /**
     * Get the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> getContent(
            String smsCampaignId, GetContentSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId)
                .addPathSegments("content");
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SmsCampaignContent.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Set the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> upsertContent(
            String smsCampaignId, UpsertContentSmsCampaignsRequest request) {
        return upsertContent(smsCampaignId, request, null);
    }

    /**
     * Set the content for an SMS campaign.
     */
    public MailchimpClientHttpResponse<SmsCampaignContent> upsertContent(
            String smsCampaignId, UpsertContentSmsCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/sms-campaigns")
                .addPathSegment(smsCampaignId)
                .addPathSegments("content");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SmsCampaignContent.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }
}
