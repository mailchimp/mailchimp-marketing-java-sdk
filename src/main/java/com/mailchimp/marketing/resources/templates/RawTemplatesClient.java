/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.templates;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.MediaTypes;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.QueryStringMapper;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.core.pagination.SyncPagingIterable;
import com.mailchimp.marketing.resources.templates.requests.CreateTemplatesRequest;
import com.mailchimp.marketing.resources.templates.requests.DeleteTemplatesRequest;
import com.mailchimp.marketing.resources.templates.requests.GetTemplatesRequest;
import com.mailchimp.marketing.resources.templates.requests.ListDefaultContentTemplatesRequest;
import com.mailchimp.marketing.resources.templates.requests.ListTemplatesRequest;
import com.mailchimp.marketing.resources.templates.requests.UpdateTemplatesRequest;
import com.mailchimp.marketing.resources.templates.types.ListDefaultContentTemplatesResponse;
import com.mailchimp.marketing.resources.templates.types.ListTemplatesResponse;
import com.mailchimp.marketing.types.TemplateInstance;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawTemplatesClient {
    protected final ClientOptions clientOptions;

    public RawTemplatesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get a list of an account's available templates.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<TemplateInstance>> list() {
        return list(ListTemplatesRequest.builder().build());
    }

    /**
     * Get a list of an account's available templates.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<TemplateInstance>> list(RequestOptions requestOptions) {
        return list(ListTemplatesRequest.builder().build(), requestOptions);
    }

    /**
     * Get a list of an account's available templates.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<TemplateInstance>> list(ListTemplatesRequest request) {
        return list(request, null);
    }

    /**
     * Get a list of an account's available templates.
     */
    public MailchimpClientHttpResponse<SyncPagingIterable<TemplateInstance>> list(
            ListTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates");
        if (request.getCount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "count", request.getCount().get(), false);
        }
        if (request.getOffset().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "offset", request.getOffset().get(), false);
        }
        if (request.getCreatedBy().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_by", request.getCreatedBy().get(), false);
        }
        if (request.getSinceDateCreated().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "since_date_created", request.getSinceDateCreated().get(), false);
        }
        if (request.getBeforeDateCreated().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "before_date_created",
                    request.getBeforeDateCreated().get(),
                    false);
        }
        if (request.getType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "type", request.getType().get(), false);
        }
        if (request.getCategory().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "category", request.getCategory().get(), false);
        }
        if (request.getFolderId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "folder_id", request.getFolderId().get(), false);
        }
        if (request.getSortField().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort_field", request.getSortField().get(), false);
        }
        if (request.getContentType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "content_type", request.getContentType().get(), false);
        }
        if (request.getSortDir().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort_dir", request.getSortDir().get(), false);
        }
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListTemplatesResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListTemplatesResponse.class);
                int newPageNumber =
                        request.getOffset().map((Integer page) -> page + 1).orElse(1);
                ListTemplatesRequest nextRequest = ListTemplatesRequest.builder()
                        .from(request)
                        .offset(newPageNumber)
                        .build();
                List<TemplateInstance> result = parsedResponse.getTemplates().orElse(Collections.emptyList());
                return new MailchimpClientHttpResponse<>(
                        new SyncPagingIterable<TemplateInstance>(
                                true, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Create a new template for the account. Only Classic templates are supported.
     */
    public MailchimpClientHttpResponse<TemplateInstance> create(CreateTemplatesRequest request) {
        return create(request, null);
    }

    /**
     * Create a new template for the account. Only Classic templates are supported.
     */
    public MailchimpClientHttpResponse<TemplateInstance> create(
            CreateTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateInstance.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get information about a specific template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> get(String templateId) {
        return get(templateId, GetTemplatesRequest.builder().build());
    }

    /**
     * Get information about a specific template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> get(String templateId, RequestOptions requestOptions) {
        return get(templateId, GetTemplatesRequest.builder().build(), requestOptions);
    }

    /**
     * Get information about a specific template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> get(String templateId, GetTemplatesRequest request) {
        return get(templateId, request, null);
    }

    /**
     * Get information about a specific template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> get(
            String templateId, GetTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates")
                .addPathSegment(templateId);
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateInstance.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a specific template.
     */
    public MailchimpClientHttpResponse<Void> delete(String templateId) {
        return delete(templateId, DeleteTemplatesRequest.builder().build());
    }

    /**
     * Delete a specific template.
     */
    public MailchimpClientHttpResponse<Void> delete(String templateId, RequestOptions requestOptions) {
        return delete(templateId, DeleteTemplatesRequest.builder().build(), requestOptions);
    }

    /**
     * Delete a specific template.
     */
    public MailchimpClientHttpResponse<Void> delete(String templateId, DeleteTemplatesRequest request) {
        return delete(templateId, request, null);
    }

    /**
     * Delete a specific template.
     */
    public MailchimpClientHttpResponse<Void> delete(
            String templateId, DeleteTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates")
                .addPathSegment(templateId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Update the name, HTML, or <code>folder_id</code> of an existing template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> update(String templateId) {
        return update(templateId, UpdateTemplatesRequest.builder().build());
    }

    /**
     * Update the name, HTML, or <code>folder_id</code> of an existing template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> update(String templateId, RequestOptions requestOptions) {
        return update(templateId, UpdateTemplatesRequest.builder().build(), requestOptions);
    }

    /**
     * Update the name, HTML, or <code>folder_id</code> of an existing template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> update(String templateId, UpdateTemplatesRequest request) {
        return update(templateId, request, null);
    }

    /**
     * Update the name, HTML, or <code>folder_id</code> of an existing template.
     */
    public MailchimpClientHttpResponse<TemplateInstance> update(
            String templateId, UpdateTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates")
                .addPathSegment(templateId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateInstance.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get the sections that you can edit in a template, including each section's default content.
     */
    public MailchimpClientHttpResponse<ListDefaultContentTemplatesResponse> listDefaultContent(String templateId) {
        return listDefaultContent(
                templateId, ListDefaultContentTemplatesRequest.builder().build());
    }

    /**
     * Get the sections that you can edit in a template, including each section's default content.
     */
    public MailchimpClientHttpResponse<ListDefaultContentTemplatesResponse> listDefaultContent(
            String templateId, RequestOptions requestOptions) {
        return listDefaultContent(
                templateId, ListDefaultContentTemplatesRequest.builder().build(), requestOptions);
    }

    /**
     * Get the sections that you can edit in a template, including each section's default content.
     */
    public MailchimpClientHttpResponse<ListDefaultContentTemplatesResponse> listDefaultContent(
            String templateId, ListDefaultContentTemplatesRequest request) {
        return listDefaultContent(templateId, request, null);
    }

    /**
     * Get the sections that you can edit in a template, including each section's default content.
     */
    public MailchimpClientHttpResponse<ListDefaultContentTemplatesResponse> listDefaultContent(
            String templateId, ListDefaultContentTemplatesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/templates")
                .addPathSegment(templateId)
                .addPathSegments("default-content");
        if (request.getFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fields", request.getFields().get(), true);
        }
        if (request.getExcludeFields().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "exclude_fields", request.getExcludeFields().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListDefaultContentTemplatesResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }
}
