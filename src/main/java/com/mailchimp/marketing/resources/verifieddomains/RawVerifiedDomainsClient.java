/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.resources.verifieddomains;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mailchimp.marketing.core.ClientOptions;
import com.mailchimp.marketing.core.MailchimpClientApiException;
import com.mailchimp.marketing.core.MailchimpClientException;
import com.mailchimp.marketing.core.MailchimpClientHttpResponse;
import com.mailchimp.marketing.core.MediaTypes;
import com.mailchimp.marketing.core.ObjectMappers;
import com.mailchimp.marketing.core.RequestOptions;
import com.mailchimp.marketing.core.RetryInterceptor;
import com.mailchimp.marketing.resources.verifieddomains.requests.CreateActionVerifyVerifiedDomainsRequest;
import com.mailchimp.marketing.resources.verifieddomains.requests.CreateVerifiedDomainsRequest;
import com.mailchimp.marketing.resources.verifieddomains.requests.DeleteVerifiedDomainsRequest;
import com.mailchimp.marketing.resources.verifieddomains.requests.GetVerifiedDomainsRequest;
import com.mailchimp.marketing.resources.verifieddomains.types.CreateActionVerifyVerifiedDomainsResponse;
import com.mailchimp.marketing.resources.verifieddomains.types.CreateVerifiedDomainsResponse;
import com.mailchimp.marketing.resources.verifieddomains.types.GetVerifiedDomainsResponse;
import com.mailchimp.marketing.resources.verifieddomains.types.ListVerifiedDomainsResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawVerifiedDomainsClient {
    protected final ClientOptions clientOptions;

    public RawVerifiedDomainsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get all of the sending domains on the account.
     */
    public MailchimpClientHttpResponse<ListVerifiedDomainsResponse> list() {
        return list(null);
    }

    /**
     * Get all of the sending domains on the account.
     */
    public MailchimpClientHttpResponse<ListVerifiedDomainsResponse> list(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/verified-domains");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListVerifiedDomainsResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Add a domain to the account.
     */
    public MailchimpClientHttpResponse<CreateVerifiedDomainsResponse> create(CreateVerifiedDomainsRequest request) {
        return create(request, null);
    }

    /**
     * Add a domain to the account.
     */
    public MailchimpClientHttpResponse<CreateVerifiedDomainsResponse> create(
            CreateVerifiedDomainsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/verified-domains");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateVerifiedDomainsResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get the details for a single domain on the account.
     */
    public MailchimpClientHttpResponse<GetVerifiedDomainsResponse> get(String domainName) {
        return get(domainName, GetVerifiedDomainsRequest.builder().build());
    }

    /**
     * Get the details for a single domain on the account.
     */
    public MailchimpClientHttpResponse<GetVerifiedDomainsResponse> get(
            String domainName, RequestOptions requestOptions) {
        return get(domainName, GetVerifiedDomainsRequest.builder().build(), requestOptions);
    }

    /**
     * Get the details for a single domain on the account.
     */
    public MailchimpClientHttpResponse<GetVerifiedDomainsResponse> get(
            String domainName, GetVerifiedDomainsRequest request) {
        return get(domainName, request, null);
    }

    /**
     * Get the details for a single domain on the account.
     */
    public MailchimpClientHttpResponse<GetVerifiedDomainsResponse> get(
            String domainName, GetVerifiedDomainsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/verified-domains")
                .addPathSegment(domainName);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetVerifiedDomainsResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a verified domain from the account.
     */
    public MailchimpClientHttpResponse<Void> delete(String domainName) {
        return delete(domainName, DeleteVerifiedDomainsRequest.builder().build());
    }

    /**
     * Delete a verified domain from the account.
     */
    public MailchimpClientHttpResponse<Void> delete(String domainName, RequestOptions requestOptions) {
        return delete(domainName, DeleteVerifiedDomainsRequest.builder().build(), requestOptions);
    }

    /**
     * Delete a verified domain from the account.
     */
    public MailchimpClientHttpResponse<Void> delete(String domainName, DeleteVerifiedDomainsRequest request) {
        return delete(domainName, request, null);
    }

    /**
     * Delete a verified domain from the account.
     */
    public MailchimpClientHttpResponse<Void> delete(
            String domainName, DeleteVerifiedDomainsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/verified-domains")
                .addPathSegment(domainName);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Verify a domain for sending.
     */
    public MailchimpClientHttpResponse<CreateActionVerifyVerifiedDomainsResponse> createActionVerify(
            String domainName, CreateActionVerifyVerifiedDomainsRequest request) {
        return createActionVerify(domainName, request, null);
    }

    /**
     * Verify a domain for sending.
     */
    public MailchimpClientHttpResponse<CreateActionVerifyVerifiedDomainsResponse> createActionVerify(
            String domainName, CreateActionVerifyVerifiedDomainsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("3.0/verified-domains")
                .addPathSegment(domainName)
                .addPathSegments("actions")
                .addPathSegments("verify");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new MailchimpClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, CreateActionVerifyVerifiedDomainsResponse.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new MailchimpClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new MailchimpClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new MailchimpClientException("Network error executing HTTP request", e);
        }
    }
}
