/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mailchimp.marketing.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AudiencesContactStatus {
    public static final AudiencesContactStatus ARCHIVED = new AudiencesContactStatus(Value.ARCHIVED, "archived");

    public static final AudiencesContactStatus ACTIVE = new AudiencesContactStatus(Value.ACTIVE, "active");

    private final Value value;

    private final String string;

    AudiencesContactStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AudiencesContactStatus
                        && this.string.equals(((AudiencesContactStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ARCHIVED:
                return visitor.visitArchived();
            case ACTIVE:
                return visitor.visitActive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AudiencesContactStatus valueOf(String value) {
        switch (value) {
            case "archived":
                return ARCHIVED;
            case "active":
                return ACTIVE;
            default:
                return new AudiencesContactStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        ARCHIVED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitArchived();

        T visitUnknown(String unknownType);
    }
}
